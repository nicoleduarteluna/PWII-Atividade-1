export let fParImpar = function (num:number) : boolean {
    return num % 2 == 0
} 

export let fArea = function (larg:number, comp:number) : number {
    return (larg * comp)
}

export let cubo =  (numero:number) : number => numero * numero * numero